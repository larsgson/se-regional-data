{
  "name": "se-regional-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Scripture Earth regional data pipeline. Pipeline is Python + Make; the single JS script scripts/map_figures.mjs uses proskomma-core to parse .pkf succinct docsets.",
  "scripts": {
    "map:figures": "node scripts/map_figures.mjs"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "proskomma-core": "^0.11.3"
  },
  "engines": {
    "node": ">=20"
  }
}
